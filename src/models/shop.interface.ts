export interface Shop {
  $key?: string,
  name: string,
  activity: string,
  town?: string,
  address?: string,
  image?: string,
  logo?: string,
  longitude?: number,
  latitude?: number
}
